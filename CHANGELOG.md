# Changelog

All notable changes to this project are documented here.

## [1.9-stable] — 2026-06-04

- Split the original single-file `sudoku9.html` into separate `index.html`, `styles.css` and `game.js` for easier maintenance.
- Added a version badge in the lower-right corner of the page.
- Added repository files: `README.md`, `TODO.md`, `CHANGELOG.md` and `.gitignore`.
- Minor copy fix on the win message.

## [Earlier]

- Original single-file build (`sudoku9.html`) with full gameplay: random puzzle generation, notes mode, highlighting, timer, best-time tracking and the number tracker.
